import { ReferralstatusiconPipe } from './referralstatusicon.pipe';

describe('ReferralstatusiconPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferralstatusiconPipe();
    expect(pipe).toBeTruthy();
  });
});
