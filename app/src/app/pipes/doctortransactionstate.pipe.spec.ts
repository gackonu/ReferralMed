import { DoctortransactionstatePipe } from './doctortransactionstate.pipe';

describe('DoctortransactionstatePipe', () => {
  it('create an instance', () => {
    const pipe = new DoctortransactionstatePipe();
    expect(pipe).toBeTruthy();
  });
});
